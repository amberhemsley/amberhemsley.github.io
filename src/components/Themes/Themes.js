import { createTheme } from '@mui/material/styles';

let theme = createTheme({
    palette: {
        primary: {
            main: '#e8bacf',
        },
        secondary: {
            main: '#bae8d2',
        },
    },
});

export default theme;